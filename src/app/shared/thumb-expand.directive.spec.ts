import { ThumbExpandDirective } from './thumb-expand.directive';

describe('ThumbExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbExpandDirective();
    expect(directive).toBeTruthy();
  });
});
