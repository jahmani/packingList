import { MorePopOverDirective } from './more-pop-over.directive';

describe('MorePopOverDirective', () => {
  it('should create an instance', () => {
    const directive = new MorePopOverDirective();
    expect(directive).toBeTruthy();
  });
});
