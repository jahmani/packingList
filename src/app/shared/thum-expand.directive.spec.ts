import { ThumExpandDirective } from './thum-expand.directive';

describe('ThumExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumExpandDirective();
    expect(directive).toBeTruthy();
  });
});
