import { StoreCurrencyPipe } from './store-currency.pipe';

describe('StoreCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
